# Decoded `source-d80.tap`

These files were decoded from PROMETHEUS compressed source records. Names beginning with `_` are D40/D80-oriented utility examples (read/write, directory traversal, file lookup, formatting, load/save helpers, and error handling). They help explain the disk environment and ROM conventions but do not match the compact resident PROMETHEUS disk driver byte-for-byte.
